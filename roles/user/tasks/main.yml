---

- include: user.yml
- include: git.yml

- name: home folders
  file: path=/home/{{ user }}/{{ item }} state=directory
  with_items:
    - Downloads
    - Pictures
    - .config/systemd/user
    - .config/systemd/scripts
  become: yes
  become_user: "{{ user }}"

