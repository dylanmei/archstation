---

- name: docker daemon
  pacman: name=docker state=latest
  notify:
    - restart docker

- name: docker extras
  pacman: name={{ item }} state=latest
  with_items:
    - docker-compose
    - docker-machine

- service: name=docker enabled=yes
