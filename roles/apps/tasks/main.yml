---

- pacman: name={{ item }} state=latest
  with_items:
    - firefox
    - thunderbird
    - chromium

- yaourt: name={{ item }} state=latest
  with_items:
    - fromscratch-bin
    - chromium-widevine
  become: yes
  become_user: "{{ user }}"

- include: chat.yml
- include: dropbox.yml
- include: develop.yml
- include: openstack.yml
- include: vmware.yml
